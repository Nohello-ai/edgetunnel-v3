{
  "name": "edgetunnel-v3",
  "version": "3.0.1",
  "description": "Cloudflare Workers edge tunnel: modular src/ + single-file dist/worker.js (VLESS/Trojan/Shadowsocks over WS/gRPC/XHTTP)",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "test": "node --test",
    "build": "node scripts/build.mjs",
    "release:notes": "node scripts/build.mjs --notes"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}
